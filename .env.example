# Civilization Node Configuration
# Copy this file to .env and adjust as needed

# ------------------------------------------------------------------------------
# DIRECTORY STRUCTURE
# ------------------------------------------------------------------------------
# The root directory for all Civilization Node data (library, models, db).
# Default: /opt/civilization
CIV_ROOT=/opt/civilization

# ------------------------------------------------------------------------------
# PORTS
# ------------------------------------------------------------------------------
# Port for Open WebUI (The main interface)
WEBUI_PORT=3000

# Port for Kiwix Server (Offline Library)
KIWIX_PORT=8080

# Port for Ollama (LLM API)
OLLAMA_PORT=11434

# ------------------------------------------------------------------------------
# HARDWARE
# ------------------------------------------------------------------------------
# Set to 'true' to skip NVIDIA GPU checks (for running on CPU or Mac)
SKIP_GPU_CHECK=false
